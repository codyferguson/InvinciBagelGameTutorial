package sample;

import javafx.animation.AnimationTimer;

public class GamePlayLoop extends AnimationTimer {
    @Override
    public void handle(long now) {
        //program logic that gegts processed on every pulse that JavaFX processes
    }

    @Override
    public void start() {
        super.start();
    }

    @Override
    public void stop() {
        super.stop();
    }
}
